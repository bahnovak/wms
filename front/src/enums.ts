export enum Routes {
  INBOUND_STOCK = 'inbound-stock',
  ORDERS = 'orders',
  PRODUCTS = 'products',
  PURCHASES = 'purchases',
  STORAGES = 'storages',
  USERS = 'users',
}

export enum Role {
  Regular = 'regular',
  Admin = 'admin',
}
